<Window x:Class="Patience.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Patience"
        xmlns:views="clr-namespace:Patience.Views"
        xmlns:viewModels="clr-namespace:Patience.ViewModels"
        mc:Ignorable="d"
        Title="Patience"
        Height="450" 
        Width="800" d:DataContext="{d:DesignInstance {x:Type viewModels:MainWindowViewModel}}">
    <Window.Resources>
        <Style TargetType="GridSplitter">
            <Setter Property="Background" Value="GhostWhite" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment" Value="Stretch" />
        </Style>
    </Window.Resources>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File">
                <MenuItem Header="Open..." />
                <MenuItem Header="Exit" />
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="About" />
            </MenuItem>
        </Menu>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="5"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <views:BindableRichTextBox Grid.Column="0" DataContext="{Binding File1}" ScrollBarVisibility="Hidden" />
            <GridSplitter Grid.Column="1"></GridSplitter>
            <views:BindableRichTextBox Grid.Column="2" DataContext="{Binding File2}" ScrollBarVisibility="Visible" />
        </Grid>
    </DockPanel>
</Window>
